let express,cors,readdirSync;_6bb‍.w("express",[["default",["express"],function(v){express=v}]]);_6bb‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_6bb‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);



const morgan = require("morgan");
const mongoose = require("mongoose");
require("dotenv").config();

// create express app
const app = express();



// apply middlewares
app.use(cors());
app.use(express.json());
app.use(morgan("dev"));

// route
readdirSync("./routes").map((r) => app.use(require('api/',`./routes/${r}`)));
readdirSync("./routes").map((r) => _6bb‍.g.console.log(r));
// const files = readdirSync("./routes").forEach((element)=>console.log(require(`./routes/${element}`)))
// require('./routes/auth')(app)



//database connection
mongoose
.connect(process.env.MONGO_URI,{})
.then(()=> console.log("DB connected"))
.catch((e)=>_6bb‍.g.console.log("DB error =>", e))

// port
const port = process.env.PORT || 8000;

app.listen(port, () => console.log(`Server is running on port ${port}`));
